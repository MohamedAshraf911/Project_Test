#include <stdio.h>
#include <stdlib.h>
#include "Head.h"

int main()
{
    int x = 10;
    int y = 20;
    printf("sum of %d and %d = %d\n",x,y,Sum_Values(x,y));
    printf("Multiply of %d and %d = %d\n",x,y,Multiply_Values(x,y));
}
